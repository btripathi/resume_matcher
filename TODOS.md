# TODOS

Generated by /plan-eng-review on 2026-03-19. Branch: main.
All items completed on 2026-03-19.

---

## COMPLETED

### 1. ✅ Add threading.RLock guard around runtime_settings (ARCH-1)
Added `rs_get()`, `rs_set()`, `rs_snapshot()` helpers backed by `threading.RLock`. All runtime accesses in `backend/app.py` use these. `update_runtime_settings` batches under `with _settings_lock:`.

### 2. ✅ Atomic deep scan checkpoint writes (ARCH-2)
Added `checkpoint_job_run()` to `database.py` — single UPDATE for payload, result, progress, and current_step. Wired through `repository.py` → `job_runner.py`.

### 3. ✅ Daemon threads + Event shutdown for heartbeat (ARCH-3)
Already implemented — heartbeat threads were `daemon=True` with `threading.Event` shutdown. No changes needed.

### 4. ✅ Enable SQLite WAL mode (ARCH-4)
Added `PRAGMA journal_mode=WAL` in `database.py` `get_connection()`.

### 5. ✅ Full pytest test suite (TEST-1)
41 tests across 6 files: `test_database.py` (11), `test_ai_engine.py` (9), `test_document_utils.py` (6), `test_api.py` (10), `test_job_runner.py` (5). Uses mock LLM and temp DBs.

### 6. ✅ Extract business logic from app.py into services (ARCH-5)
Moved auto-push logic to `GitHubSyncService`, queue pause recomputation to `JobRunner.recompute_paused()`. `app.py` has thin wrappers only. Writer auth kept as local helper (only 3 callers).

### 7. ✅ Decompose deep scan method in analysis.py (CQ-1)
Extracted `_extract_criteria_items`, `_normalize_deep_eval`, `_run_bulk_deep_scan`, `_run_per_requirement_deep_scan` as class methods. `score_match` reduced from ~560 to ~235 lines.

### 8. ✅ Deduplicate _join_tags (CQ-2)
Removed standalone `_join_tags` from `repository.py`. Now calls `self.db._join_tags()`.
